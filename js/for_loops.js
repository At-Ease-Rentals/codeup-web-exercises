'use strict'

function showMultiplicationTable (num1, i) {
    for (let i = 0; i <= 10; i++){

        //multiple i with user  input number
        const showMultiplicationTable = i * number;

        // display the result
        console.log(`${number}  *  ${i} = ${showMultiplicationTable}`);
    };
}